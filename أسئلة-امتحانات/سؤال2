#include <stdio.h>
#include <math.h>

/* 
  اكتب برنامجا لإيجاد وطباعة حاصل جمع المتسلسلة الآتية
  2, 4, 8, 16, 32, 64 ... 2^n
*/

int main() {
  int sum, n;

  puts("Input n value:");
  scanf("%d", &n);
  
  for(int i=1; i<=n; i++) {
    sum += pow(2,i);
  }
  printf("Sum = %d\n", sum);
}
